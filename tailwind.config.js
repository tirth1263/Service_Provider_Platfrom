export const content = ["./src/**/*.{js,jsx,ts,tsx}"];
export const theme = {
  extend: {}, // You can extend the default Tailwind theme here if you need custom colors, fonts, etc.
};
export const plugins = [];
